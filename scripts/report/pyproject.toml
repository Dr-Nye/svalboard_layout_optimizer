[project]
name = "report"
version = "0.1.0"
description = "Parse keyboard layout optimization results and generate comprehensive CSV, SVG, and markdown reports"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "rich>=14.1.0",
    "typer>=0.17.4",
]
